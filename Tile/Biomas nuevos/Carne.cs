using Microsoft.Xna.Framework;
using Terraria;
using Terraria.ID;
using Terraria.ModLoader;

//using NightWorld2.Content;
/*
namespace NightWorld.Tiles
{
    internal class Carne : ModTile
    {
        public override void SetStaticDefaults()
        {
            //TileID.Sets.Stone = true;

            Main.tileSolid[Type] = true;
            Main.tileMergeDirt[Type] = true;
            //Main.tileBockLight[Type] = true;
            Main.tileShine[Type] = 900;
            Main.tileShine2[Type] = true;
            Main.tileSpelunker[Type] = true;
            Main.tileOreFinderPriority[Type] = 350;

            AddMapEntry(new Color(200, 200, 200), CreateMapEntryName());

            DustType = DustID.Tungsten;
            ItemDrop  tModPorter Note: Removed. Tiles and walls will drop the 
                      * item which places them automatically. Use RegisterItemDrop to alter 
                      * the automatic drop if necessary.  =
                      ModContent.ItemType<Content.Items.Bioma.Carne>();
            //SoundType = SoundID.Tink;
            //SoundStyle = 1;

            MineResist = 1.5f;
            MinPick = 80;
        }
    }
}
*/